package LinkedLists;

public class Node {
    int data;
    Node next;

    Node (int data){
        this.data = data;
    }
    
}












//In Linkedlist we don't have to specify the size of the list as linked list is a dynamic data structure and it automatically changes size when an element is added or removed